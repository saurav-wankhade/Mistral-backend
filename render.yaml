services:
  - type: web
    name: mistral-backend
    env: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: MISTRAL_API_KEY
        sync: false
